#include <stdio.h>

int main(int argc, char const *argv[])
{
    do
    {
        for (int i = 0; i < count; i++)
        {
            /* code */
        }
        for (size_t i = 0; i < count; i++)
        {
            /* code */
        }
        for (size_t i = 0; i < count; i++)
        {
            /* code */
        }
        
        
    } while ((i<=9) && (j<=3) && (k<=3));
    

    return 0;
}